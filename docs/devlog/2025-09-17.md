## Realtime Notes
- Plan to introduce `base_species` to the schema for my pokemon table to accomplish two goals:
    - Can prevent showing duplicate entries for pokemon with multiple forms (arceus, type:null)
    - Can use base_species to get movepools for gmax and mega forms, rather than storing duplicates
- Plan to introduce `prevo_id` to the schema for my pokemon table to allow movepools to be propagated forward through evolutionary lines. The way this will work is that the learnset for the (evolved) pokemon will be loaded from the learnset json, then I'll query its prevo's movepool, and for each move not already in the pokemons own movepool, bring it forward. Undecided as to whether or not that will be simple runtime logic or if I should pull it forward in the database right away. Leaning toward runtime to avoid rigidity in available data.