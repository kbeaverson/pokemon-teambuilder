## Realtime Notes
- Adding `json_serializable` methods (from `freezed`) to all models to handle to/from json code generation for debugging purposes
- Added `none` as an option for move learn method so that there would be a distinct default value
    - Added sprite for none (just in case, don't plan to use)
- Importing powersync testing binary
- Bug in powersync testing binary prevents me from being able to perform unit testing on my repo methods