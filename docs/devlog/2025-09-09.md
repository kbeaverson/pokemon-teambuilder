## Realtime notes
- Fixed pokemon model to decode json array for type rather than a comma separated list stored as a string
- Did the same as above for item category and move type
- Created csvs of each data type via python json/csv package, and stored them in supabase