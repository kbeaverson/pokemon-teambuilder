## Realtime Notes
- Created PokemonCardContext classes for pokemon, movepoolentries, and abilitypoolentries
- Created BuildAroundBehavior enum for code clarity with respect to the above
- What I am doing right now is establishing the foundational buildaround elements for the PokemonInfoCard widget described in earlier writings. Once this is complete I can begin designing the PokemonInfoCard widget to take this context attribute, which will be passed to it from every context in which it is used. I envision circumstances where PokemonInfoCard as:
    - From the Pokedex screen
    - The pokemon list for moves
    - The pokemon list for abilities
- The multiprovider from the homepage doesn't currently work. Implementing dex screens with local versions of the repo and I'll circle back to fix later.
- Fixed retrieval of stats from database. Now decodes json before querying each stat.
- Add BST to PokemonViewModel.
- Used copilot to leap forward pokedex screen presentation, filtering, and search so that I have an example to work from for future index screens.